# DevEquality

> DevEquality — это социальная инициатива, цель которой — сделать процесс разработки доступным для людей с нарушением зрения и пользователей, которые не могут использовать клавиатуру и мышь.

https://docs.google.com/document/d/1vmau9fUgVhKV2pH6Q0wbGFyHZWR6cxDJDGXoTXB4KXs/edit?tab=t.0

Проект представляет собой голосовую среду разработки, в которой все взаимодействие с проектами, файлами и кодом осуществляется голосом.
Все результаты могут озвучиваться (для слабовидящих) и визуализироваться (для тех, кто не может использовать руки).

## 🌟 Основная идея

Пользователю не нужно взаимодействовать с клавиатурой или мышью при выполнении следующих задач:

*   Создание проектов
*   Работа с файлами и папками
*   Редактирование и запуск Python-кода
*   Получение аудио-описаний и аудио-результатов
*   `git clone` существующих репозиториев

Все команды пользователь произносит голосом.
Все результаты система озвучивает, интерфейс полностью hands-free.

## 🚀 MVP-функциональность

На этапе MVP система поддерживает:

*   Создание проекта
*   Клонирование git-проекта
*   Создание папки
*   Создание файла
*   Редактирование файла
*   Запуск Python-скриптов + озвучивание вывода
*   Чтение содержимого файла
*   Краткое описание (summary) файла

## 🧠 Архитектура и принцип работы

Проект состоит из двух частей:

**1. Клиент (Windows)**

Скрипт `microphone.py`:
*   включает микрофон и управляется клавишей `Пробел`;
*   записывает речь пользователя;
*   сохраняет аудио;
*   отправляет имя файла на сервер (WSL).

**2. Сервер (WSL)**

Скрипт `main.py`:
*   принимает имя аудиофайла;
*   создает и обновляет состояние (`AgentState`);
*   обрабатывает аудио с помощью локальной ASR (NVIDIA NeMo);
*   отправляет текст в LLM для определения операции;
*   выполняет нужный узел графа (создание, запуск, редактирование и т.д.);
*   формирует текст для озвучивания;
*   озвучивает результат с помощью локальной NeMo TTS;
*   отправляет сигнал клиенту, что команда завершена.

## 📦 Состояние графа (AgentState)

Состояние включает:
```
class AgentState(TypedDict):
    messages: list                  # история сообщений для LLM
    audio_filepath: str             # путь к текущему аудио
    transcribed_message: str        # распознанная команда
    curr_project: str               # текущий проект
    curr_dir: str                   # текущая директория
    curr_file: str                  # текущий файл
    text_to_pronounce: str          # текст, который нужно озвучить
    audio_to_pronounce: str         # путь к сгенерированному аудио
   ```

## 🔄 Логика графа LangGraph

Основные узлы:

*   **transcribe** — локальная ASR (NeMo)
*   **get\_operation** — LLM определяет операцию и объект
*   **create\_project, create\_file, edit\_file, ...** — операции
*   **synthesize** — локальная TTS (NeMo)
*   **END** — завершение цикла

## 🎤 Как работает взаимодействие (сценарий)

### 1. Создание проекта

**Клиент говорит:**
> "Create project my first project"

**Результат:**
*   создается директория `projects/MyFirstProject`
*   озвучивается: *"Your project my first project is created"*

### 2. Создание файла

**Клиент:**
> "Create file main.py"

**Результат:**
*   создается файл `main.py`
*   ответ: *"File main.py was created"*

### 3. Голосовое редактирование

**Клиент диктует форматированный код:**
> "import time enter"
> "x equals five enter"
> "for i in range open parentheses one comma ten close parentheses colon enter"
> "tab x plus equals two enter"
> "print open parentheses x close parentheses"

**В итоге в `main.py` будет:**
```python
import time
x = 5
for i in range(1, 10):
    x += 2
print(x)
```
### 4. Запуск кода

**Клиент:**
> "Run main.py"

**Результат:**
> "Result of your script is 23"

## 🛠️ Установка и запуск

### Требования

*   Python 3.12
*   Windows + WSL2

### 🔧 Установка (client — Windows)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r client_windows/requirements.txt
```

### 🔧 Установка (server — WSL)

```bash
pip install nemo_toolkit['asr']
pip install nemo_toolkit['tts']
pip install langchain langgraph langchain-google-genai
pip install tavily
```

## ▶️ Запуск

### В терминале WSL:
```bash
cd server_wsl
python main.py
```

### В терминале Windows:
```bash
cd client_windows
python microphone.py
```